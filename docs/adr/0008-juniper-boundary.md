# ADR 0008: Juniper is an optional client

Notes is useful without Juniper. Future integration will use a stable typed command
surface, explicit scope, optimistic write hashes, revision-before-write, and an audit
trail. Integration defaults to disabled.
