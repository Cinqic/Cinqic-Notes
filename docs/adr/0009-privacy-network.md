# ADR 0009: Offline by default

The app has no network feature in this milestone: no telemetry, remote fonts, uploads,
automatic sync, or model calls. CSP and capabilities remain narrow so adding a network
feature requires a deliberate architecture and privacy review.
