{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Narrow desktop capability: dialogs and app commands only; no blanket filesystem or shell access.",
  "windows": ["main"],
  "platforms": ["linux", "windows", "macOS"],
  "permissions": ["core:default", "dialog:default"]
}
