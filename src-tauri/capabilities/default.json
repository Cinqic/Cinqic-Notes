{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Narrow desktop capability: dialogs, app commands, and destroying the main window after a save completes on close. No filesystem, shell, or network access is granted to the UI.",
  "windows": ["main"],
  "platforms": ["linux", "windows", "macOS"],
  "permissions": ["core:default", "dialog:default", "core:window:allow-destroy"]
}
